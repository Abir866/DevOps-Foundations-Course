# TODO: Define the Workflow Name
name: devops-workflow

# TODO: Define triggers for the workflow
on:
   push:
     branches: master 
   pull_request:
     branches: master

jobs:
  # TODO: Define a job for building and testing the frontend
  frontend:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Node
      uses: actions/setup-node@v2
      with:
        node-version: '14'
      # TODO: Checkout code
      # TODO: Setup Node.js
      # TODO: Install dependencies
      # Run tests
    - name: Test code
     # run: node Project/frontend/src/App.test.js
    - name: Test Setup
      run: node Project/frontend/src/setupTests.js
      # TODO: Build the React app


      # TODO: Add deployment steps
